---
interface Frontmatter {
  title: string;
  description?: string;
}

const posts = await Astro.glob<Frontmatter>("../markdown/*.md");
---

<div class="w-full text-center p-2 overflow-y-scroll">
  <div class="flex flex-col gap-4">
    {posts.map(({ Content }) => <Content />)}
  </div>
</div>
